<head >
    <meta charset="utf-8">
    <title>Appli Gift</title>
    <link rel="stylesheet" href="/css/output.css" >
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <div class="navbar flex justify-around py-4  bg-gray-800 text-stone-300">
       <div class="flex justify-around gap-x-6 ">
            <a class="hover:text-red-200 " href="{{ url_for('newBoxesForm') }}">Box</a>
            <a class="hover:text-red-200" href="{{ url_for('prestations') }}">Prestation</a>
            <a class="hover:text-red-200" href="{{ url_for('categories') }}">Catégories</a>
            <a class="hover:text-red-200" href="{{ url_for('acceuil') }}"></a>
        </div>
        <div class="logo hover:text-red-200">
            <h>Gift Box</h>
        </div>

        <div class="right flex justify-around gap-x-6 ">
            <a class=" mx-6 hover:text-red-200" href="{{ url_for('acceuil') }}"><i class="fas fa-shopping-cart "></i></a>

            {% if session.utilisateur is defined %}
                <a class="mx-6 hover:text-red-200" href="{{ url_for('acceuil') }}"><i class="fa fa-user"></i></a>
            {% else %}
                <a class="mx-6 hover:text-red-200" href="{{ url_for('connection') }}">Connectez-vous !</a>
            {% endif %}

        </div>
    </div>
</head>
